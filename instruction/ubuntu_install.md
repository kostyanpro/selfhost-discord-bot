# 🐧 Установка на Ubuntu

## 1. Скачивание архива проекта

Скачать архив со всеми файлами необходимыми для запуска бота можно [здесь](https://github.com/kostyanpro/selfhost-discord-bot/releases). Все остальные шаги выполняйте из директории файлов бота

> **💡 Важно:** Скачивайте последнюю доступную версию во избежание ошибок

## 2. Установите Python 3.13

Убедитесь, что у вас установлен **Python 3.13**. 
Если **Python** не установлен:

```bash
sudo apt update
sudo apt install python3.13-full
sudo apt install python3-venv -y
```

## 3. Создание виртуального окружения

```bash
bash setup_venv.sh
```

## 4. Запуск веб-интерфейса

```bash
bash start.sh
```

## 5. Настройка аудио

```bash
sudo apt install ffmpeg
which ffmpeg
```
В терминале выведится путь до **FFMPEG** - укажите его в веб-интерфейсе

---

*Продолжение настройки смотрите в основном README.md*